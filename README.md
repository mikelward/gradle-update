# jvm-update

Weekly Gradle dependency batches for mikelward's JVM/Android repos, as a
reusable GitHub Actions workflow — the sibling of mikelward/npm-update for
repos built on Gradle version catalogs. Content arrives via pull requests.
